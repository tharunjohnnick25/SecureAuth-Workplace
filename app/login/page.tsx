import { Login } from '@/components/pages/Login';

export const metadata = {
  title: 'Login | SecureAuth Workplace',
  description: 'Premium multi-factor risk-based authentication system',
};

export default function LoginPage() {
  return <Login />;
}
